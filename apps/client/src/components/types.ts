export type SpawnResult = {
    backend_id: string;
    drone: null;
    secret_token: string;
    spawned: boolean;
    status: string;
    status_url: string;
    subdomain: string;
    token: string;
    url: string;
};